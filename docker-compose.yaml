---

services:

  www:
    image: northamerica-northeast1-docker.pkg.dev/manafont-www-dev/github-readme-stats/www:latest
    build:
      context: .
      dockerfile: Dockerfile
    platform: linux/amd64
    ports:
      - "3000:3000"
    command: ["npm", "start"]
    env_file:
      - .env
